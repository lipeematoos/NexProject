# NETPROJECT — variáveis de ambiente (nunca versionar segredos reais)
DATABASE_URL=postgresql://user:pass@localhost:5432/netproject
JWT_SECRET=troque-este-valor-em-producao
JWT_REFRESH_SECRET=troque-este-valor-em-producao
AI_PROVIDER=nex-deterministic
AI_API_KEY=
APP_URL=http://localhost:5173
NODE_ENV=development
MAIL_DRIVER=log
EVENT_BROKER=none
